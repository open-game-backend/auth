spring:
  h2:
    console:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

de:
  opengamebackend:
    auth:
      provider:
        github:
          redirectUri: http://localhost:8080/login
          clientId:
          clientSecret:
